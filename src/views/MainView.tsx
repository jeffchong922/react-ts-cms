import React from 'react'
import { Button } from 'antd'

const MainView = () => {
  return (
    <div>
      MainView <Button type='primary'>antd 按钮</Button>
    </div>
  )
}

export default MainView
